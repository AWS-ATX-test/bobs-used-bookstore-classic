@using Bookstore.Domain.ReferenceData
@using Bookstore.Web.Helpers

@model Bookstore.Web.Areas.Admin.Models.ReferenceData.ReferenceDataIndexViewModel

@{
    ViewData["Title"] = "Reference Data";
}

<div class="d-flex m-3">
    <h5 class="me-auto">Reference Data</h5>

    <a asp-area="Admin" asp-controller="ReferenceData" asp-action="Create" class="btn btn-primary">New Reference Data Item</a>

</div>

<div class="card mx-3">
    <div class="card-body">
        <form class="row row-cols-lg-auto g-3 align-items-center" method="get">
            <div class="col-12">
                <label class="visually-hidden" for="referenceDataTypeSelect">Reference data type</label>

                <!-- [PORT-TODO: replace EnumDropDownListFor with Html.DropDownListFor + Enum.GetValues or a <select asp-for/asp-items> tag helper] -->
                <select asp-for="Filters.ReferenceDataType" class="form-select">
                    <option value="">All Reference Data Types</option>
                    @foreach (var value in Enum.GetValues<ReferenceDataType>())
                    {
                        <option value="@value">@value</option>
                    }
                </select>

            </div>

            <div class="col-12">
                <button type="submit" class="btn btn-primary">Filter</button>
            </div>

            <div class="col-12">
                <a asp-area="Admin" asp-controller="ReferenceData" asp-action="Index" class="btn btn-secondary">Clear</a>
            </div>
        </form>
    </div>
</div>

<div class="card m-3">
    <div class="card-header">
        <div class="d-flex justify-content-end">
            <partial name="_Paginator" />
        </div>
    </div>

    <div class="card-body">
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th scope="col">Type</th>
                    <th scope="col">Text</th>
                    <th scope="col"></th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Items.Count() == 0)
                {
                    <tr>
                        <td colspan="10" rowspan="4" style="text-align:center;">
                            <span>No Updates Currently</span>
                        </td>
                    </tr>
                }
                else
                {
                    foreach (var item in Model.Items)
                    {
                        <tr>
                            <td>@item.ReferenceDataType</td>
                            <td>@item.Text</td>
                            <td>
                                <a asp-area="Admin" asp-controller="ReferenceData" asp-action="Update" asp-route-id="@item.Id" class="card-link">Update</a>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>

    <div class="card-footer">
        <div class="d-flex justify-content-end">
            <partial name="_Paginator" />
        </div>
    </div>
</div>
